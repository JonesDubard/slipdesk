/**
 * Interactive Demo seeder entrypoint.
 * Implementation lives in `seed-demo.mjs` (runs with plain Node).
 *
 *   npm run seed:demo
 *   npm run seed:demo -- --reset
 *
 * Requires migration `0009_demo_company_flag.sql` applied first.
 */
export {};
